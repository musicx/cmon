# Compound Context Bundle

## Accepted Work Summary

The routing matrix refresh proved that a harness repo's shortest operator-facing surfaces must evolve as internal stage discipline evolves. Otherwise the repo teaches an older workflow truth than the one it actually enforces.

## Existing Knowledge Surface

- Existing Solution Path: `docs/solutions/workflow/operator-facing-routing-matrix-for-core-skills-2026-04-07.md`

## Candidate Lesson

- routing artifacts should surface mature stage-internal gates when those gates materially change operator behavior
- multi-surface parity is a valid reason to treat docs work as having non-local effects
